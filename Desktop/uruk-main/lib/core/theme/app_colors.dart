import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Brand Colors (from Uruk logo)
  static const Color primary = Color(0xFF1A3A8F);
  static const Color primaryDark = Color(0xFF0F2660);
  static const Color primaryLight = Color(0xFF2E5FD1);
  static const Color primarySurface = Color(0xFFE8EDF7);

  // Background
  static const Color background = Color(0xFFF5F7FF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF0F1F45);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status Colors
  static const Color success = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);
  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color warning = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color info = Color(0xFF0284C7);
  static const Color infoLight = Color(0xFFE0F2FE);

  // UI Elements
  static const Color divider = Color(0xFFE5E7EB);
  static const Color border = Color(0xFFD1D5DB);
  static const Color borderFocused = Color(0xFF1A3A8F);
  static const Color disabled = Color(0xFFD1D5DB);
  static const Color disabledText = Color(0xFF9CA3AF);

  // Subscription tiers
  static const Color standard = Color(0xFF1A3A8F);
  static const Color shared = Color(0xFF7C3AED);
  static const Color vip = Color(0xFFD97706);

  // Shadow
  static const Color shadow = Color(0x1A1A3A8F);
}
